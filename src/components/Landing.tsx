import React from 'react';
import { ROUTES } from '../utils/routing/routes';

const Landing: React.FC = () => {
	return (
		<div>
			Landing Coming Soon
			<br />
			<a href={ROUTES.REGISTER}>Register to Home</a>
		</div>
	);
};

export default Landing;
